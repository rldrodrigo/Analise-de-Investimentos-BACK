version: '3'

services:
  web:
    build: ./web
    ports:
      - "5000:5000"
    links:
      - db
  db:
    build: ./db
  front:
    image: analise-ativos
    build: ../analise-de-investimentos
    ports:
      - "80:80"